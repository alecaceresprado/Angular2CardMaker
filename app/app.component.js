/*(function(app) {
  app.AppComponent =
    ng.core.Component({
      selector: 'head',
      template: '<title>learn</title>'
    })
    .Class({
      constructor: function(view) {
        this.title = "learn";
        ng.platform.browser.bootstrap(view);
      }
    });
})(window.app || (window.app = {}));
*/